/**
 * Habilidades avulsas do Bouncer.
 *
 * Vazio de propósito: toda regra de autorização deste projeto é sobre um
 * recurso e tem alvo, então mora em `app/policies/`. Uma `ability` solta é para
 * o caso sem alvo ("pode ver o painel de métricas"), e ainda não há nenhum.
 *
 * O arquivo continua existindo porque `initialize_bouncer_middleware.ts` o
 * importa com `import * as abilities`.
 */
export {}
